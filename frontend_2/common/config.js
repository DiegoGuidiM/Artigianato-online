/* Global config shared by all pages
manage communications with api and JWTs for logins/registers
*/
window.CONFIG = {
  API_BASE_URL: 'http://localhost:3000/api',
  STORAGE: { TOKEN: 'cw_token', USER: 'cw_user' }
};

const CONFIG = {
  API_BASE_URL: 'http://localhost:3000/api',
  ASSETS_BASE_URL: 'http://localhost:3000'  
};
